<Window x:Class="Jobnet.Views.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Jobnet"
        Width="400" Height="160"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" Grid.Row="0"
                   Margin="0,0,0,8"
                   FontSize="13"
                   TextWrapping="Wrap"/>
        <TextBox x:Name="InputBox" Grid.Row="1"
                 Padding="6,4"
                 BorderBrush="#CCC"
                 KeyDown="InputBox_KeyDown"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Padding="14,4" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="OK" Padding="14,4" IsDefault="True" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
